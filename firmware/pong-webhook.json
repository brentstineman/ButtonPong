{
    "event": "register",
    "url": "https://<apidns>.azurewebsites.net/api/pong",
    "requestType": "PUT",
    "noDefaults": true,
    "rejectUnauthorized": true,
    "json": {
        "deviceId": "{{PARTICLE_DEVICE_ID}}",
        "status": "{{PARTICLE_EVENT_VALUE}}"
    },
    "query": {
        "code": "<masterhostkey>"
    }
}